

int removeDuplicates(vector<int> &arr, int n) {
	int h=n;
	for(int i=1;i<n;i++){
          if (arr[i] > arr[i - 1]) {
            h = h;
          } else {
            h = h - 1;
          }
                }
		return h;
	}

